Breeding Range: <%- for region in species.regions -%><%= h(region.name) %> (<%= link_to h(region.code), main_url(:action => 'abbreviations') %>)<% if region != species.regions.last %>, <% end %><%- end -%><%- if !species.breeding_subregions.blank? -%>. Subregions: <%= h(species.breeding_subregions) %><% end %><%- if !species.nonbreeding_regions.blank? -%>. Nonbreeding Range: <%= h(species.nonbreeding_regions) %><%- end -%>